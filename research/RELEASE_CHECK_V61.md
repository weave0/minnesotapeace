# v6.1 release acceptance notes

Public acceptance targets for this cleanup:

- `/guide/` must not contain `A global reader can understand almost every case update`.
- `/guide/` must not use `Why outsiders should care` as a public section label.
- `/guide/` court-language copy must distinguish accusation, conviction, sentencing, restitution, forfeiture and dismissal directly.
- `/good/` must load `/js/good-map-v61.js` through the publication middleware.
- The map correction must use a boundary-derived Minnesota silhouette and city-coordinate pins for all 12 story locations.
- Existing public-origin checks for homepage/status/good/guide/oversight/record and private `/research/` must still pass.
